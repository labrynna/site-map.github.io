# Google Maps API Key
# Get this from: https://console.cloud.google.com/apis/credentials
# Required API: Maps JavaScript API
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Google Sheets Authentication (Choose ONE method)

# OPTION 1: API Key (Simpler, but requires publicly accessible sheet)
# Get this from: https://console.cloud.google.com/apis/credentials
# Required API: Google Sheets API
# Note: Your Google Sheet must be publicly accessible (Share > Anyone with the link can view)
GOOGLE_SHEETS_API_KEY=your_google_sheets_api_key_here

# OPTION 2: Service Account (Recommended for private sheets)
# This should be the entire JSON key file content from Google Cloud Console
# Get this from: https://console.cloud.google.com/iam-admin/serviceaccounts
# Format: {"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}
GOOGLE_SHEETS_CREDENTIALS={"type":"service_account","project_id":"..."}

# Google Sheets Spreadsheet ID (Required)
# This is the ID from your Google Sheets URL
# Example: https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit
GOOGLE_SHEETS_ID=your_spreadsheet_id_here

# Google Sheets Range (Optional)
# Default: Sheet1 (reads all data from first sheet)
# If you have a specific sheet name or want to limit range, specify it here
# Format: SheetName or SheetName!A:D
# Examples: 
#   - Sheet1 (reads all data from Sheet1)
#   - Locations (reads all data from sheet named "Locations")
#   - Sheet1!A:D (reads columns A through D from Sheet1)
GOOGLE_SHEETS_RANGE=Sheet1

# Column Headers:
# The function automatically detects columns based on header names in the first row.
# Supported header names (case-insensitive):
#   - Address/Location -> Address column
#   - Lat/Latitude -> Latitude column
#   - Long/Longitude/Lng -> Longitude column
#   - Picture taken -> Visit status column
# If no headers are detected, assumes column order: Address, Latitude, Longitude, Picture taken

# Note: For Google Drive access, the service account credentials (GOOGLE_SHEETS_CREDENTIALS)
# can also be used if you share the Drive files/folders with the service account email
